# Ignore the entire build directory (compiled binaries, object files, cache)
build/

# Ignore all generated CSV files anywhere in the project
*.csv

# Ensure compile_commands.json in the root directory is kept
!/compile_commands.json
